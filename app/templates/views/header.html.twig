<nav class="navbar navbar-expand-lg bg-dark border-bottom border-gray" data-bs-theme="dark">
    <div class="container">
        <div class="d-flex align-items-center justify-content-between w-100">
            <div class="text-white">
                24/7 Customer service <strong>1-000-000-0000</strong>
            </div>

            <div class="d-flex justify-content-end flex-grow-1">
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <div class="collapse navbar-collapse flex-grow-1" id="navbarSupportedContent">
                    <ul class="navbar-nav me-auto mb-2 mb-lg-0 justify-content-end w-100">
                        {% if app.user and app.user.isVerified %}
                            <li class="nav-item">
                                <a class="nav-link {{ '/purchases' in app.request.uri ? 'active' : '' }}" href="{{ path('app_purchase_index') }}">Purchases</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link {{ '/account' in app.request.uri ? 'active' : '' }}" href="{{ path('app_account') }}">Account</a>
                            </li>
                            {% if app.user.isAdmin %}
                                <li class="nav-item">
                                    <a class="nav-link {{ '/admin' in app.request.uri ? 'active' : '' }}" href="{{ path('admin_index') }}">Admin</a>
                                </li>
                            {% endif %}
                        {% endif %}

                        <li class="nav-item">
                            <a class="nav-link {{ '/shipping-and-return' in app.request.uri ? 'active' : '' }}" href="/shipping-and-return">Shipping & Return</a>
                        </li>

                        <li class="nav-item">
                            <a class="nav-link {{ '/track-order' in app.request.uri ? 'active' : '' }}" href="/track-order">Track Order</a>
                        </li>

                        {% if not app.user %}
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('app_login') }}">Login</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('app_register') }}">Register</a>
                            </li>
                        {% else %}
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('app_logout') }}">Logout</a>
                            </li>
                        {% endif %}
                    </ul>
                </div>
            </div>
        </div>
    </div>
</nav>

<nav class="navbar navbar-expand-lg bg-dark app-main-header" data-bs-theme="dark">
    <div class="container">
        {% if '/admin' in app.request.uri %}
            <a class="navbar-brand" href="/admin">Admin</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link {{ '/admin/products' in app.request.uri ? 'active' : '' }}" href="{{ path('admin_product_index') }}">Products</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {{ '/admin/categories' in app.request.uri ? 'active' : '' }}" href="{{ path('admin_category_index') }}">Categories</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {{ '/admin/pages' in app.request.uri ? 'active' : '' }}" href="{{ path('admin_page_index') }}">Pages</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {{ '/admin/users' in app.request.uri ? 'active' : '' }}" href="{{ path('admin_user_index') }}">Users</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {{ '/admin/orders' in app.request.uri ? 'active' : '' }}" href="{{ path('admin_order_index') }}">Orders</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_home') }}">Back home</a>
                    </li>
                </ul>
            </div>
        {% else %}
            <div class="d-inline-flex align-items-center">
                <img src="{{ app_logo }}" alt="{{ app_name }}" class="me-2" width="24">
                <a class="navbar-brand fw-bold text-uppercase" href="{{ path('app_home') }}">{{ app_name }}</a>
            </div>
            <div class="d-flex align-items-center justify-content-end">
                <form class="d-flex" role="search" action="{{ path('app_home') }}" method="get">
                    <input class="form-control me-2" type="search" name="s" placeholder="Search products..." aria-label="Search" value="{{ app.request.query.get('s') }}">
                    <button class="btn btn-outline-success" type="submit">Search</button>
                </form>
                <div class="text-white ms-3 cart-btn-wrapper">
                    <button class="app-cart-btn">
                        <img src="{{ asset('assets/img/cart.svg') }}" alt="Cart">
                        <span>0</span>
                    </button>
                </div>
            </div>
        {% endif %}

    </div>
</nav>